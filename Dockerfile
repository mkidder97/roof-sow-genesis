# Legacy Dockerfile - Moved to frontend/Dockerfile and backend/Dockerfile
# This file kept for reference but no longer used in docker-compose

# For development, use:
# - frontend/Dockerfile for React/Vite frontend
# - backend/Dockerfile for Python API

# Production builds can be added later with:
# - frontend/Dockerfile.prod
# - backend/Dockerfile.prod

echo "Please use frontend/Dockerfile or backend/Dockerfile instead"
exit 1